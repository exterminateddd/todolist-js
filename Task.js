class Task {
    constructor(title, text) {
        this.title = title;
        this.text = text;
    }
    setText(newText) {
        this.text = newText;
    }
    setTitle(newTitle) {
        this.title = newTitle;
    }
}